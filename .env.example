# --- Django ---
DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=True

# --- Database (Docker) ---
DB_NAME=aiassistant
DB_USER=aiuser
DB_PASSWORD=aipass
DB_HOST=postgres
DB_PORT=5432

# --- Celery/Redis (Docker) ---
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1

# --- Public base URL (dev: set to your tunnel domain; prod: your domain) ---
PUBLIC_BASE_URL=https://your-public-domain-or-tunnel

# --- Twilio (SMS) ---
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# --- Email ---
# For real Gmail sending, switch backend and fill SMTP + app password:
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=you@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
# EMAIL_USE_TLS=True

# For dev (prints emails to console logs):
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
NOTIFY_EMAIL=you@example.com

